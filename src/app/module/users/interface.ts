export type ITokenData = {
    id: string
    email: string
}

export type loginResponse = {
    accessToken: string
    expires_in:string
}

export type registerResponse = {
    accessToken: string
}